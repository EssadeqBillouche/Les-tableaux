#include"stdio.h"

main(void)
{
        int i;
        int Tab[5] = {11, 12, 13, 14, 15};
        for (i = 0; i < 5; i++)
        {
                printf(" Le normber No %d est %d \n", i+1, Tab[i]);
        }
        return 0;
}
